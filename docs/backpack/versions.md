# 版本

本页给出背包版各组件的版本基线,以及 XTac-UMI Collector 各版本的对外要点。版本号以你自己设备上读到的为准:采集单元与夹爪固件看控制台 系统 → [设备信息](system.md#device-info),头显在 Pico 设置里看,平板在系统设置里看。

## 版本基线 {#baseline}

按 XTac-UMI Collector 0.3.16 配套。整套设备的软硬件版本要对应,升级任一组件后回到这张表核对。

| 组件 | 基线 | 在哪看 / 怎么升 |
|---|---|---|
| 平板 OS(小米平板) | 建议 3.0.303 及以上 | 平板 设置 → 我的设备;随系统更新 |
| 数采背包 OS 固件 | V1.2.0 | 出厂预装,由技术支持升级 |
| XTac-UMI Collector(采集单元) | 0.3.16 | 控制台 系统 → [设备信息](system.md#device-info)「采集端版本」;升级见[升级与 OTA](update.md) |
| Pico OS | 建议 5.15.5.U 及以上 | 头显 设置 → 通用 → 关于;升级见 [Pico4 头显与追踪器](../common/pico4.md#pico-system) |
| XTac-UMI XR(头显应用) | 0.2.5 | 头显应用库;安装与升级见 [Pico4 头显与追踪器](../common/pico4.md#pico-app) |
| 主夹爪固件 | V1.2.2 | 控制台 系统 → [设备信息](system.md#device-info)「夹爪 MCU」;升级在 系统 → [夹爪配置](system.md#gripper) |

背包版的采集单元升级全部在控制台里完成,不需要 PC。PC 版的版本基线是另一套,见 [PC 版 · 版本与升级](../pc/versions.md#required)。

## 各版本要点 {#history}

0.3.9 起,每次升级完成后首次进入控制台会展示这一版的更新内容。0.3.x 系列的对外要点(本站正文按上面的基线 0.3.16 编写,更高版本先列在这里备查):

| 版本 | 日期 | 要点 |
|---|---|---|
| 0.3.20 | 2026-09-10 | 上传后端新增 NFS 与 FTP/FTPS 两档;上传配置页新增实际读写检测,保存或改过配置后检测状态失效、需要重测;任务进度卡片在数据总量之外新增当天新增数据,按本地日期统计,目录容量文案改为「数据总量」并含已归档数据;上传配置格式随新后端升级,旧版本认不出新增的后端类型,回退前先备份配置或移除新后端及其任务绑定 |
| 0.3.19 | 2026-09-10 | Pico 连续 5 秒收不到任何位姿与双目画面时,即使 TCP 会话还在线也判定采集数据链路中断,自动走正常停录收尾并播报「Pico 连接失败」——这条只管「什么都没收到」,与 0.3.17 那条「收到了但位姿无效」是两套独立机制,仍有位姿或画面到达时不触发;SoftAP 候选信道由两组扩为四组,未连外部 WiFi 时按设备身份固定选一组,分散多台背包热点的信道占用(改动 2026-08-31 已随代码发出,本版补记入发布说明);修复插着 Pico 时背包的外网出口被 Pico 抢占(部分较早出厂的设备如需获得这项修复,需联系技术支持处理,控制台升级包不会自动完成这项更新,见[控制台升级包覆盖不到的更新](update.md#system-patch));修复用夹爪删除录制后任务计数与 episode 列表不刷新,按键调试改为显示来自哪只夹爪 |
| 0.3.18 | 2026-09-08 | 开录时把头显的双目分辨率、左右目内参与左右目外参,连同 Head SN 与 XUMI 软件版本一起冻结进这条 episode,随 H264 与训练 MCAP 保留,导出 LeRobot 时写进逐 episode 的 `meta/xumi_collection_devices.json`;历史录制不反推,开录时这批参数不全只告警,不阻止开录 |
| 0.3.17 | 2026-09-08 | 实时监控的触觉预览画面口径统一,不同浏览器看到的结果一致(回放的画面与录制、导出的数据都没有变化);头显 TCP 还连着、但 Tracker 关机或持续上报无效位姿时,不再当成短时出视野,超过防抖窗口即按正常停录收尾并播报 Pico 连接失败;录制中用右夹爪单击标记 subtask,标记写入成功后新增提示音,该提示音不会抢占录完或断联的播报;修复结束预览或刷新页面后预览会话不释放 |
| 0.3.16 | 2026-09-02 | 每条录制记住实际使用的采集设备:开录时固定背包、头显、左右夹爪、左右 tracker 与六路相机的 SN 与软件版本,导出的 LeRobot 数据集新增 `meta/xumi_collection_devices.json` 可按 episode 追溯;设备信息页新增头显详情;新增 STS 训练 MCAP 上传;语音播报新增音量滑块;修复 LeRobot `info.json` 字段顺序不稳定 |
| 0.3.15 | 2026-08-31 | 尚未标定的腕部相机现在也会做画面去畸变,改用该型号镜头的通用参数矫正;通用参数不含单台镜头的个体差异,精度不及逐台标定,该侧状态仍显示「未标定」,补做标定后自动改用本台参数 |
| 0.3.14 | 2026-08-31 | 腕部画面去畸变补记入发布说明(取景与边缘投影可选,默认关闭,只影响之后的新录制);「采集模式」与「按键绑定」合并为「设置 › 采集设置」一页;指示灯说明改成会动的对照表;「录满目标条数」的拦截改为与进度显示同一口径,按累计采集计算 |
| 0.3.13 | 2026-08-31 | 修复语音播报没有声音的问题:提示与逐句试听现在都能正常从设备喇叭发出 |
| 0.3.12 | 2026-08-27 | 设备开始用语音播报关键节点(开始录制、一条录完、录制中出现严重问题、录制中夹爪或头显掉线),不需要平板在旁边;可一键关闭,开关旁可逐句试听 |
| 0.3.11 | 2026-08-27 | 录制多了「已归档」这一档状态(已就绪 / 已上传 / 已归档 / 已丢弃);「清盘」更名为「归档」且随时可用;上传过的录制可以正常回放;导出窗口改成先选去向再配置,上传改为从下拉里选事先配置好的上传后端 |
| 0.3.10 | 2026-08-27 | 导出可以分别选「导出去向」与「导出格式」,`LeRobot 数据集` 与 `mcap` 两种格式并列、互不排斥;下载得到的是一个压缩包;上传完成后默认不再自动删除本地文件,清理时会保护还没用过的格式;单条下载入口统一到任务级导出 |
| 0.3.9 | 2026-08-25 | 支持远程集中升级,可由管理端指定版本下发、分批发布;升级不打断进行中的录制,界面显示下载进度与倒计时且不遮挡采集画面;升级后首次进入展示更新内容;支持远程重启与回退上一版本 |
| 0.3.8 | 2026-08-21 | 修复 Pico 时钟同步在系统对时后失效、头显时间轴错位导致 LeRobot 导出报时间范围不重叠;末端外参默认使用出厂内置值,导出的 TCP 位姿不再是裸追踪器位姿 |
| 0.3.7 | 2026-08-20 | LeRobot 数据集新增 `observation.tracker_confidence` 列,逐帧记录追踪器置信度;录制状态行折行显示不再溢出;项目页、手机端与系统子页一批界面修整 |
| 0.3.6 | 2026-08-19 | 任务级统一「导出」对话框:勾选 episode 批量下载 MCAP(raw / 转码),LeRobot 只走发布;移除任务级 ZIP 导出与条目行「下载 H264」(单条下载移到回放页);任务目标条数达到后拒绝开录,开录失败改为弹窗提示 |
| 0.3.5 | 2026-08-17 | 发布到 ModelScope 支持发布后自动清盘,默认开启,可勾选保留本地文件 |
| 0.3.4 | 2026-08-17 | 用夹爪按键或另一台设备开停录时,任务条数与录制列表实时刷新;归档 H264 编码参数调整,体积回落 |
| 0.3.3 | 2026-08-17 | 头显位姿进入 LeRobot 数据集(state / action 20 维 → 29 维);修复头显时间轴回跳导致整场导出失败;回放支持暂停、拖动进度与弱网码率自适应,并与实时预览整机互斥 |
| 0.3.2 | 2026-08-13 | OTA 升级后自动修复运行环境,老设备也能通过 OTA 装上新版,配合 A/B 回滚保护 |
| 0.3.1 | 2026-08-12 | 弱网下预览按单路自愈,不再整体黑屏;预览分辨率减半降低 CPU 占用,录制仍是全分辨率 |
| 0.3.0 | 2026-08-12 | 监控页新增「当前任务条数」卡片与任务目标;录制编号任务内持久;新增 系统 → 上传配置多档凭据;录制盘用满 80% 拒绝开录;追踪器失效指示与录制质检;系统页默认打开设备信息 |

升级步骤与回滚见[升级与 OTA](update.md);升级遇到问题联系[支持](../common/reference.md#support)。
